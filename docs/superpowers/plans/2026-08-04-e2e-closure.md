# 全量端到端验证与文档收口（M8）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用参考流程走通全链路（下载→拼接→黑白→人工选择→双钩→选择→三版本瓦片→Manifest），核对 AC-01…AC-16，更新 TASKLIST/CONTEXT/CHANGELOG（T8.1–T8.3）。

**Architecture:** 新增 `tests/test_e2e_pipeline.py`（本地 HTTP 服务 + CLI + 评审 TestClient 的组合端到端测试）；文档收口更新 TASKLIST、CONTEXT、CHANGELOG。

**Tech Stack:** 同 M7（conda env `zgy_hd_py311`、pytest、FastAPI TestClient）。

## Global Constraints

- 端到端测试不得访问外网：来源图片由本地 HTTP 服务提供（AGENTS.md 可重复验证）。
- 全量质量门禁：pytest / ruff / black / isort / mypy 全绿（AGENTS.md G4）。
- 文档同步：TASKLIST 进度、CONTEXT 术语、CHANGELOG 变更（AGENTS.md D/F）。

---

### Task 1: 全链路端到端测试

**Files:**
- Create: `tests/test_e2e_pipeline.py`

**Interfaces:**
- Consumes: `main(...)`（download/stitch/bw/hook/tile/manifest）、`create_app`（评审选择）、`validate_manifest`。

- [ ] **Step 1: 写测试**（2 个不同高度来源图 → 高度统一拼接 → 黑白候选 → 评审选 bw → 双钩候选 → 评审选 hook → 三版本瓦片 → Manifest；断言状态、对象键、x_range、URL 模板、缩略图倒序）
- [ ] **Step 2: 运行确认通过**
- [ ] **Step 3: 提交**

```bash
git add tests/test_e2e_pipeline.py
git commit -m "test(REQ-calligraphy-hd-image-system): 全链路端到端测试"
```

---

### Task 2: 文档收口

**Files:**
- Modify: `TASKLIST-calligraphy-hd-image-system.md`（进度与阶段状态）
- Modify: `CONTEXT.md`（金字塔层级公式、多图窄区间规则）
- Modify: `CHANGELOG.md`（M8 实际变更）

- [ ] **Step 1: 更新三份文档**
- [ ] **Step 2: 全量门禁复跑**
- [ ] **Step 3: 提交并推送**

```bash
git add TASKLIST-calligraphy-hd-image-system.md CONTEXT.md CHANGELOG.md
git commit -m "docs(REQ-calligraphy-hd-image-system): M8 端到端验证完成与文档收口"
```

---

## Self-Review 结论

- 端到端覆盖 AC-01（下载记录）、AC-02/03/04（拼接与映射/高度统一）、AC-05/06（黑白候选）、AC-07（双钩）、AC-08（三版本瓦片一致性）、AC-09/10/13/14（Manifest 契约）、AC-15（评审选择记录）、AC-16（对象键布局）。
