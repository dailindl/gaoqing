# CONTEXT：书法高清大图系统（术语与已确认决策）

## 领域术语（已确认）

- **输入清单**：JSON 数组，每项 `{img_id, source_url, img_num}`；`img_num`（1 起始）是多图拼接的权威排序字段，清单数组排序以 `img_num` 为准。
- **来源图 `img_id`**：输入清单中每张来源图的唯一标识（实测为 UUID），仅用于来源追溯，不用于存储分区。
- **作品级 `version_id`**：存储分区与 Manifest 使用的作品标识，与来源图 `img_id` 不同；由任务参数提供。
- **不做特殊处理（缩略图高度不足 1920）**：不放大、不填充、不强制裁切，按实际高度输出，此时不强制 9:16。
- **三版本原始大小**：原图、黑白图、双钩图保持拼接原图最终画布尺寸；拼接时唯一允许的缩放是高度统一等比缩放。
- **候选图保留策略**：人工选择完成前全部候选全分辨率保留；整套作品处理完毕后，未选候选可删除，已选候选及参数/策略记录永久保留。
- **Manifest 版本**：`manifest_version` 当前为 1，移动端据此做兼容判断。
- **金字塔层级公式**：`Zmax = ceil(log2(max(W,H)/256))`；层级尺寸 `round(W·2^(z-Zmax))`、`round(H·2^(z-Zmax))`；三版本共用同一公式与网格。
- **多图缩略图窄区间**：分屏图 X 区间不足 1080 时，缩略图宽度取区间宽度（不放大），裁取区域必须落在该分屏图区间内。
