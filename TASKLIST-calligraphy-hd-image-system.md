# 任务列表：书法高清大图系统（REQ-calligraphy-hd-image-system V1.2）

> 依据需求 V1.2（已确认）生成；每项标注关联章节与验收标准（AC）。待技术方案确定项（JPEG 质量、双钩算法、性能 SLO）在方案阶段输出后细化。

## 阶段 0：方案与基建

- **T0.1** 方案设计文档：影响范围、备选方案（≥2 个）与优劣对比、每步验证方式（REQ §12；AGENTS.md B）
- **T0.2** 开源调研：瓦片金字塔生成、大图拼接、书法双钩/轮廓提取的成熟实现评估（AGENTS.md C）
- **T0.3** conda 环境 `zgy_hd_py311`：FastAPI、Pillow、pytest、black、isort、ruff、mypy 等依赖（REQ §12）
- **T0.4** 确定待定项：JPEG 编码质量与色彩配置、双钩图算法与质量指标、性能 SLO（REQ §15.2）

## 阶段 1：获取与归档（REQ §4）

- **T1.1** 输入清单解析与校验：JSON 数组契约 `{img_id, source_url, img_num}`；`img_num` 权威排序；单图/多图判定（§4.1；AC-01）
- **T1.2** 下载与归档：HTTP 下载、失败原因记录、JPG/PNG/TIF 可读性与尺寸校验（§4.2；AC-01、AC-04b）
- **T1.3** 来源与处理记录：`source_index`、`img_id`、`source_url`、下载结果、本地文件标识、原始尺寸、统一后尺寸、X 区间（§4.2–4.3；AC-01、AC-04）
- **T1.4** 失败处理：下载失败/不可读/尺寸越界时停止并保留阶段原因，不发布 Manifest（§4.3；AC-04b）

## 阶段 2：原图拼接与导航（REQ §5）

- **T2.1** 高度统一：取 `min_h` 等比缩放，高质量重采样最小化质量损失，来源记录标注（§4.3、§5.1；AC-04）
- **T2.2** 从右向左拼接：以 `img_num` 顺序为权威，第 1 项最右、末项最左，原点 `(0,0)`（§5.1；AC-02）
- **T2.3** 来源位置映射：物理 X 区间、各层 tile-X 覆盖范围、中心点 tile-X（§5.2；AC-03）
- **T2.4** 尺寸策略：三版本保持拼接原图原始大小，唯一缩放为高度统一等比缩放（§5.1、§6、§7.2）

## 阶段 3：黑白图（REQ §6.1）

- **T3.1** 黑白候选生成：2–10 个、候选标识+参数/策略标识+时间+路径记录（§6.1；AC-05）
- **T3.2** 质量边界校验：白底黑字、笔画/印章为 0、背景 255、极性统一；0/255 约束仅限 master PNG（§6.1；AC-06）
- **T3.3** 候选保留：选择完成前全分辨率保留；整套处理完毕后未选候选可删除（§3.2、§9.1）

## 阶段 4：书法双钩图（REQ §6.2）

- **T4.1** 双钩候选生成：输入已选黑白图、2–10 个、候选记录（§6.2；AC-07）
- **T4.2** 尺寸一致性：双钩图与原图/黑白图同画布同坐标（§6.2；AC-07、AC-08）

## 阶段 5：人工评审工具（REQ §11）

- **T5.1** 候选展示：同类型 2–10 候选并排比较、缩放、细节查看（§11；AC-15）
- **T5.2** 坐标对照：候选与原图/上游正式图同一物理坐标对照（§11；AC-15）
- **T5.3** 选择确认：记录候选标识、参数/策略、路径、时间、操作者；状态流转（§6.3、§11；AC-15）
- **T5.4** 触发瓦片生成：确认后触发或允许触发正式瓦片生成（§11；AC-09）

## 阶段 6：瓦片、缩略图与 Manifest（REQ §7、§8、§10）

- **T6.1** 金字塔瓦片：256×256、Z0…Zmax 全分辨率、三版本同一坐标契约与网格（§7；AC-08）
- **T6.2** 缩略图：9:16、1080px 右→左分段、末段 `[0,1080]` 允许重叠；高度不足 1920 不做特殊处理（§8.1–8.3；AC-11、AC-12）
- **T6.3** `jump2x`：按区域中心换算各层 tile-X，覆盖全部层级且合法（§8.4；AC-13）
- **T6.4** Manifest 装配：`manifest_version`、`version_id`、`layers` 单次、URL 模板状态规则、`thumbnails` 倒序（§10；AC-09、AC-10、AC-14）
- **T6.5** 三版本一致性校验：同一 `(z,x,y)` 物理区域一致（§7.2；AC-08）

## 阶段 7：存储与迁移（REQ §9）

- **T7.1** 对象键规范：以 `version_id` 分区的正式资产键实现（§9.2；AC-16）
- **T7.2** 候选对象键：版本类型+候选标识+参数/策略标识命名（§9.2；AC-16）
- **T7.3** 迁移支持：统一根前缀、可预测对象键，便于整体迁移 OSS（§9.1；AC-16）

## 阶段 8：全量验证与记录（AGENTS.md G、D）

- **T8.1** 端到端测试：单图/多图样例走通 下载→拼接→瓦片→缩略图→Manifest（AC-01…AC-16）
- **T8.2** 质量门禁：`pytest` + `ruff` + `mypy` + `black` + `isort` 全量通过（AGENTS.md G4）
- **T8.3** 文档同步：更新 CHANGELOG/CONTEXT，提交信息关联需求编号（AGENTS.md D3）
